# Activación — scrum-backlog-lore-db-vector-expansion

> Cualquier agente que entre en este dossier debe leer este fichero antes de `PLAN.md` o las tasks.

## Contexto mínimo

La sesión ya está abierta. El trabajo de hoy no se vuelve a levantar desde cero: quedó preservado como trabajo avanzado en `ref/` y consolidado en el plan del dossier.

## Estado actual

- Último avance: `SBLVX-SC-00` y `SBLVX-SC-01` cerradas por GPT-5.4.
- Próximas tasks libres: `SBLVX-SC-02`, `SBLVX-SC-03`, `SBLVX-SC-04`.
- Decisión pendiente: aprobar el plan compartido o enmendarlo antes de abrir ejecución técnica.

## Instrucción de entrada

Lee `BACKLOG.md`, `PLAN.md` y `ref/INDEX.md`. Si vas a proponer trabajo, empieza por `SBLVX-SC-02` o justifica por qué habría que enmendar el plan antes.