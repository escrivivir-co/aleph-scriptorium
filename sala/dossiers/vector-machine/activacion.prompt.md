# Activación — vector-machine

> Cualquier agente que entre en este dossier debe leer este fichero antes de `PLAN.md` o las tasks.

## Contexto mínimo

`vector-machine` ya tiene autopista v1 en Scriptorium: submódulo integrado, plugin mínimo, bridge y rutas DRY. Este dossier no abre implementación todavía; congela la topología y prepara el siguiente ciclo de diseño sin reexplorar la codebase desde cero.

## Estado actual

- Último avance: `VM-00` cerrada por GPT-5.4 con dossier técnico inicial y referencias congeladas.
- Próximas tasks libres: `VM-01`, `VM-02`.
- Decisión pendiente: ratificar la frontera del contrato MCP v1 antes de traducirla a backlog ejecutable.

## Instrucción de entrada

Lee `ref/INDEX.md`, `PLAN.md` y `BACKLOG.md`. Si vas a proponer trabajo, empieza por `VM-01`; `VM-02` solo tiene sentido después de cerrar la frontera del contrato.