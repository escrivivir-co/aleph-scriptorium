# PROMPT 4 · HISTORIA 3 — "INGESTA Y FEDERACIÓN"

> Agente generador de clip **4 de 8**. Lee primero `00_CONTEXTO_COMPARTIDO.md`.
> Este archivo manda sobre el plano concreto; el contexto manda sobre universo, tono y prohibiciones (§6).

---

## ⬆️ ARCHIVOS A SUBIR A GEMINI OMNI — 5 de 10 máx.
> Súbelos a mano, uno a uno, junto con este prompt. NO superar 10 por vídeo.

1. `NETWORK_TOPO2.png` — `MATERIALES/BOT_MARK_NETWORK_BUILDER/`
2. `NETWORK_SYS2.png` — `MATERIALES/BOT_MARK_NETWORK_BUILDER/`
3. `fundacion-banner.png` — `MATERIALES/RUDE_SKIN_BOTS/`
4. `pics_dossier6.png` — `SPOT_DOSSIER/pics_dossier/` (federación Pub.Rooms)
5. `pics_dossier5.png` — `SPOT_DOSSIER/pics_dossier/` (cadena IACM / plomería)

---

## Ficha técnica
- **Rol en la campaña:** entrada del **Future Pipeline Engine**. La maquinaria empieza a comer datos.
- **Duración:** 10 s.
- **Pilar dominante:** Future Engine (entrada) — sobre base de Information System.
- **Posición en montaje:** tras el tercer "Skank" (clip 8).

## Intención
Mostrar **cómo entra la materia prima**: la crew **ingiere** el directo, lo **transcribe**
y lo hace viajar **asíncrono y federado** por la red. Es el plano más "técnico" del spot,
pero contado como *plomería de crew*: cables, válvulas, flujo. La idea fuerza: **tú lideras
la fiesta física; los rude bots corren el riddim de transmedia por debajo, sobre Node-RED y
Scuttlebutt.**

## Guion visual (abierto, "a partir del contenido de la imagen…")
- A partir del contenido de `NETWORK_TOPO2.png` y `NETWORK_SYS2.png`,
  compón una **mesa de nodos tipo Node-RED**: cables que se conectan, un flujo que baja por
  tuberías, paquetes de texto que avanzan por la red.
- Como capa de apoyo, evoca **una interfaz de app en modo oscuro** (sin texto legible):
  parámetros con viñetas y un **log de ejecución** corriendo por un lateral, texturizada a
  halftone, nunca como captura plana.
- A partir de `fundacion-banner.png`, deja que la palabra **`ASÍNCRONO`** se **selle en la
  pared** y parpadee al ritmo del bajo.
- Texto rústico impreso sobre el flujo: `media-extraction`, `Node-RED`, `Scuttlebutt (SSB)`.
- Sugerir la **transcripción** como una cinta de texto que se imprime y se cataloga.

## Assets recomendados
- `MATERIALES/BOT_MARK_NETWORK_BUILDER/NETWORK_TOPO2.png`
- `MATERIALES/BOT_MARK_NETWORK_BUILDER/NETWORK_SYS2.png`
- `MATERIALES/RUDE_SKIN_BOTS/fundacion-banner.png`

## Esquemas técnicos de referencia (federación y cadena de bots)
- `pics_dossier/pics_dossier6.png` — **federación Pub.Rooms**: `Peer Client → Caddy Proxy
  (WSS 443) → Scriptorium Rooms (Node-RED) → PUBLIC_ROOM`, con handshake y auth. Recorta la
  línea WSS y el handshake: la "tubería" real por la que entra el flujo, asíncrona y federada.
- `pics_dossier/pics_dossier5.png` — **cadena IACM** `bot-rabbit (ingesta) → bot-spider
  (red RETRO) → bot-horse (canal)`. Es literalmente la **plomería**: úsala como tubería por
  la que viaja el texto ingerido.

## Audio
- El **bajo dub domina la mezcla**; la guitarra mantiene el skank.
- Sensación de caudal constante, agua que corre por las tuberías del sistema.

## Voz en off (Toaster)
> "Tú lideras la fiesta. Nosotros corremos el ARG por debajo.
> Ingestamos el directo, lo transcribimos… y lo soltamos asíncrono sobre la red."

## Rótulos (stencil, español, opcionales)
- `ASÍNCRONO` · `media-extraction` · `Node-RED` · `SSB` · `WSS`

## Prompt Gemini Omni (one-line, EN)
> 10-second rocksteady clip, deep dub bass dominating the mix with steady offbeat skank over
> vinyl crackle; from the content of `NETWORK_TOPO2.png` and `NETWORK_SYS2.png`
> build a Node-RED-style patch table where cables connect and text packets
> flow down pipes like plumbing; as a support layer evoke a dark-mode app interface with no
> legible text (bulleted parameters and an execution log scrolling on one side), in halftone,
> never a flat screenshot; from `fundacion-banner.png` the word "ASÍNCRONO" stamps on a
> wall and pulses with the bass; overlay real schematics as faded blue blueprint: the
> Pub.Rooms WSS federation handshake from `pics_dossier6.png` and the IACM chain
> bot-rabbit→bot-spider→bot-horse from `pics_dossier5.png` as actual pipework; a transcript
> ribbon prints and gets filed; hand-lettered tags: media-extraction, Node-RED, SSB; halftone,
> smudged ink, stencil; calm constant flow, tactical; NO clean dashboards, NO sci-fi, NO
> sound-system imagery.

## Recordatorio de prohibiciones
- Maquinaria **táctil y analógica**, no dashboards limpios ni sci-fi.
- Sin alusiones a sonido físico: lo que corre por las tuberías es **información**.
