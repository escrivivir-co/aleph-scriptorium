# PROMPT 10 · FUERZAS — "NEWTON, EMERGENCIA Y ℵ₀=ℵ₀=ℵ₀"

> **GALERADA AUTOCONTENIDA · Omni nace de cero aquí. No necesitas abrir ningún otro archivo.**
> Todo lo necesario para llenar 10 s está abajo. El payload que se pega en Omni es el bloque ⑥.

---

## ① CONTEXTO DEL CLIP (compacto)

- **Pack:** clip **2 de 5** (~50 s en total) — *showcase* de la NETWORK-ENGINE en el look & feel
  del spot **Scriptorium Skins**.
- **Este clip:** **segundo movimiento.** Va **tras** P9 (ya hay un sistema de referencias) y
  **antes** de P11 (Apertura).
- **Arco completo:** 9 Génesis → **10 Fuerzas** → 11 Apertura → 12 Hilbert → 13 Oráculo.
- **Tesis (brújula):** *"saber no es ver, saber es correr."*
- **Tono/estética:** ska/**rocksteady 1969**, pausado, *cool*. Tinta gruesa, **stencil**, **halftone**,
  fanzine, **sello de caucho**, grano de celuloide, vinilo. Negro tinta + crema + acento (verde botella).
  Cámara mínima y firme, cortes al compás.
- **Voz:** **Toaster** grave, a contratiempo, **esparcida**.
- **Audio DE ESTE clip:** **groove completo** — bajo dub + **batería seca** + guitarra al contratiempo
  (el "drop" templado: el sistema cobra pulso, sin acelerarse).
- **Rótulos:** **español, verbatim**. Las **fórmulas** se dibujan a mano (stencil grueso). Jerga de
  código = textura técnica, no prosa.
- **PROHIBIDO (duro):** sin Trojan / sound-system / altavoces · sin neón / HUD / sci-fi / glow · sin
  hype / EDM / épica · sin capturas planas · rótulos solo en español · **no** usar el claim del spot de skins.

---

## ② ARCHIVOS A SUBIR A OMNI (7 de 10 máx.)
> Súbelos a mano, uno a uno, junto con el bloque ⑥.

1. `NETWORK_TOPO2.png` — `MATERIALES/BOT_MARK_NETWORK_BUILDER/` (malla con masas)
2. `NETWORK_TOPO_2.png` — `MATERIALES/BOT_MARK_NETWORK_BUILDER/` (variante topológica)
3. `pics_dossier5.png` — `SOURCE/pics_dossier/` (cadena de bots — fuerzas que mueven la red)
4. `pics_dossier4.png` — `SOURCE/pics_dossier/` (máquina de estados)
5. `pics_dossier3.png` — `SOURCE/pics_dossier/` (secuencia de sesión activa)
6. `SCRIPTORIUM_SKINS.png` — raíz de `ALEPH/` (sello de esquina, discreto)
7. `aleph-scriptorium-banner.png` — `MATERIALES/RUDE_SKIN_BOTS/`

---

## ③ STORYBOARD 10 s (reparto de tiempo)

- **0–3 s — masas.** La malla de P9 (`NETWORK_TOPO2.png` / `NETWORK_TOPO_2.png`) **se carga de
  masas**: nodos que se hunden, aristas tensas como cuerdas. Flechas vectoriales a mano. Entra el groove.
- **3–7 s — emergencia + cardinalidad (BEAT DOMINANTE).** Manchas de acuarela (electricidad,
  magnetismo) **impregnan** el espacio entre nodos. Secuencia ℕ → ℤ → ℚ que **parece crecer**… pero
  un **sello de caucho** golpea: **`ℵ₀ = ℵ₀ = ℵ₀`** y **`ACOTADO ≠ PEQUEÑO`**.
- **7–10 s — frontera cuántica.** El átomo central se difumina en **nube de probabilidad** (tinta que
  parpadea). Blueprints de `pics_dossier4/3.png` se encadenan al groove.

---

## ④ RÓTULOS

- **Prosa-sello / fórmula (ES, verbatim):** `ℕ ⊂ ℤ ⊂ ℚ` · `ℵ₀ = ℵ₀ = ℵ₀` · `ACOTADO ≠ PEQUEÑO`
- **Jerga de máquina (textura técnica):** `IMPACT_FORCE` · `STABLE → CRITICAL` · `NetworkTransportEvent`
  · `pathLength ≠ cardinalSlot` · `friends.hops: 3`
  - *Newton = evento `IMPACT_FORCE`* que lleva el nodo de `STABLE` a `CRITICAL`.
  - **Rótulo corazón — `pathLength ≠ cardinalSlot`:** longitud de camino y cardinalidad ℵ son
    **independientes**. Por eso ℕ=ℤ=ℚ=ℵ₀: misma **correspondencia**, distinta **métrica**. *No es
    espacio: es correspondencia.* Acotado = horizonte `friends.hops: 3`, no tamaño.
  - *Nube cuántica* = la deuda del relay (al pie, casi invisible: `PubSubHub: no reenvía`).

---

## ⑤ VOZ EN OFF (Toaster, grave, a contratiempo, esparcida)

> "Newton llena la malla… de masas.
> Pero acotado… no es pequeño.
> El átomo… ya no es punto."

---

## ⑥ ★ PROMPT GEMINI OMNI (one-line, EN) — PEGAR ESTO

> 10-second 1969 rocksteady clip, mid-tempo groove (dub bass + offbeat skank guitar + dry snare)
> over vinyl crackle, calm and tactical; from the uploaded mesh images, a network loads with masses —
> nodes sinking, edges tensing like weighted strings, hand-drawn force vectors; watercolor bleeds
> (electricity, magnetism) impregnate the space between nodes; a visual sequence ℕ → ℤ → ℚ appears to
> grow but a rubber stamp slams the dominant beat: "ℵ₀ = ℵ₀ = ℵ₀" and "ACOTADO ≠ PEQUEÑO" in thick
> hand-drawn stencil, plus a faint monospace stamp "pathLength ≠ cardinalSlot"; the central atom then
> diffuses into a pulsing probability cloud; faded blue blueprints stamp on the offbeat; Spanish
> stencil captions, verbatim, do not translate; halftone, smudged ink, fanzine edges; NO neon, NO
> sci-fi HUD, NO glow, NO speakers.

---

## ⑦ ✎ CIERRE EDITORIAL (detalles finales — revisar antes de render)

- [ ] Duración **10 s**; el **beat dominante** (3–7 s) es el sello `ℵ₀ = ℵ₀ = ℵ₀` / `ACOTADO ≠ PEQUEÑO`.
- [ ] Fórmulas en **stencil español verbatim** (símbolos ℕ ℤ ℚ ℵ₀ correctos).
- [ ] **7 imágenes** subidas; el logo solo asoma discreto en esquina.
- [ ] Audio = **groove completo** (sube respecto a P9, sin acelerar).
- [ ] `pathLength ≠ cardinalSlot` legible un instante (es la idea "no es espacio, es correspondencia").
- [ ] La malla = **maquinaria táctil**, nunca HUD sci-fi ni neón.
- [ ] Cero altavoces / cultura de sonido físico.
