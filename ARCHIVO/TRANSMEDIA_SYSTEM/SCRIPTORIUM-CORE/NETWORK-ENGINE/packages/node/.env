NODE_NAME=NodeServer
CORE_NAME=CoreEngine
PORT=3000

# --- Ontotext GraphDB (W3C SPARQL 1.1 Protocol / RDF4J REST) ---
# Example connection values for the GraphDbStore adapter. Do NOT commit secrets.
GRAPHDB_ENDPOINT=http://localhost:7200
GRAPHDB_REPOSITORY=network-engine
# Optional overrides / credentials (leave unset for an open local repo):
# GRAPHDB_UPDATE_ENDPOINT=http://localhost:7200/repositories/network-engine/statements
# GRAPHDB_USERNAME=
# GRAPHDB_PASSWORD=
