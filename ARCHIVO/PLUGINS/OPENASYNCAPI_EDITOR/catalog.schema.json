{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OpenAsyncAPI + MCP Catalog Schema",
  "description": "Schema for the OpenAPI, AsyncAPI, and MCPSpec catalog used by OpenAsyncAPI Editor",
  "type": "object",
  "required": ["version", "updated_at", "specs"],
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "updated_at": {
      "type": "string",
      "format": "date-time"
    },
    "specs": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/Spec"
      }
    }
  },
  "definitions": {
    "Spec": {
      "type": "object",
      "required": ["id", "project", "type", "spec_version", "api_version", "title", "origin", "status", "cataloged_at"],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9-]+$",
          "description": "Unique identifier in kebab-case"
        },
        "project": {
          "type": "string",
          "description": "Source project name"
        },
        "type": {
          "type": "string",
          "enum": ["openapi", "asyncapi", "mcpspec"],
          "description": "Specification type (OpenAPI, AsyncAPI, or repository-local MCPSpec descriptor)"
        },
        "spec_version": {
          "type": "string",
          "description": "Version of the format or local descriptor edition (e.g., 3.1.0 for OpenAPI, 1.0.0 for local MCPSpec descriptors)"
        },
        "protocol_version": {
          "type": ["string", "null"],
          "description": "Official protocol release identifier when the source specification uses protocol-version semantics (e.g., MCP 2025-11-25)"
        },
        "api_version": {
          "type": "string",
          "description": "Version of the API itself"
        },
        "title": {
          "type": "string",
          "description": "API title from spec"
        },
        "description": {
          "type": "string",
          "description": "API description"
        },
        "origin": {
          "type": ["string", "null"],
          "description": "Original path in repository"
        },
        "local": {
          "type": ["string", "null"],
          "description": "Local copy path in plugin data directory"
        },
        "status": {
          "type": "string",
          "enum": ["draft", "validated", "deprecated", "error"],
          "description": "Validation status"
        },
        "cataloged_at": {
          "type": "string",
          "format": "date-time"
        },
        "validated_at": {
          "type": ["string", "null"],
          "format": "date-time"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Descriptive tags for filtering"
        }
      }
    }
  }
}
